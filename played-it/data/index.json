---
---
  {% site.pages %}
  {% for item in site.pages%}
  {
    "item": "{{ item | escape }}",
    "url": "{{ item.url | escape }}",
    "path": "{{ item.path | escape }}",
    "dir": "{{ item.dir | escape }}"
  }{% endfor %}
